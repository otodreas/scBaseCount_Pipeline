[project]
name = "scbasecount-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.12"
dependencies = [
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.8",
    "pyarrow>=23.0.1",
    "scanpy>=1.12",
]
